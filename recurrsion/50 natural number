#include <stdio.h>

void printNumber(int num){
    
    if(num==0)
        return;
    printNumber(num-1);
    printf("%d\n",num);
}

int main()
{
    int num;
    scanf("%d",&num);
    printNumber(num);
    return 0;
}
