#include <stdio.h>
int main()
{
    int num,remender;
 
    printf("enter the number : ");
    scanf("%d", &num);
    remender = num % 2;
    if (remender == 0)
        printf("\n%d is an even number", num);
    else
        printf("\n%d is an odd number", num);
}
