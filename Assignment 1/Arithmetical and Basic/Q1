#include <stdio.h>

int main()
{
    int arr[10];
    int k;
    printf("the number in arre are");
    for(k=0;k<10;k++){
        printf("element of erray are %d",k);
        scanf("%d",&arr[k]);
    }
    return 0;
}
