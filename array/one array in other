#include <stdio.h>

int main()
{
    int num,i;
  scanf("%d",&num);
  int a1[num];
  for(i=0;i<num;i++){
      scanf("%d",&a1[i]);
  }
  int a2[num];
  for(i=0;i<num;i++){
      a2[i]=a1[i];
      printf("%d",a2[i]);
  }
}
